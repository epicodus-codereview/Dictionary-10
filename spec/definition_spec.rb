require('rspec')
require ('definition')

describe(Definition) do
end

describe('#part_of_speech') do
  it ("returns the part of speech of the definition") do
  test_definition = Definition.new("noun", "moister condensed from the atmosphere that falls visibly in separate drops")
  end
end
